apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: nginx
spec:
  hosts:
  - nginx
  gateways:
  - nginx-gateway
  http:
#  - match:
#    - headers:
#        post-num:
#          regex: (?!.*0$)
#    route:
#    - destination:
#        host: nginx
#        subset: num0
  - match:
    - url:
      prefix: /test/
  - route:
    - destination:
        host: nginx
        subset: num1
