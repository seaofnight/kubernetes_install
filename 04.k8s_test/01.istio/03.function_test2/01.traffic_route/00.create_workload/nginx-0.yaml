---
apiVersion: apps/v1 
kind: Deployment
metadata:
  name: nginx-0
  labels:
   postnum: "0"
   app: nginx
spec:
  selector:
    matchLabels:
      app: nginx
      postnum: "0"
  replicas: 1 
  template:
    metadata:
      labels:
        app: nginx
        postnum: "0"
    spec:
      containers:
      - name: nginx
        image: nginx:1.7.9
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx
    postnum: "0"
  name: nginx-0
  namespace: default
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx
    postnum: "0"
  sessionAffinity: None
  type: ClusterIP
